NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
NEXT_PUBLIC_STELLAR_NETWORK=testnet
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional: set this only if you want to enforce one exact wallet address.
NEXT_PUBLIC_REQUIRED_FREIGHTER_ADDRESS=
# Optional server-only override used by Next API proxy route.
# In production, set BACKEND_URL to your backend origin.
BACKEND_URL=http://localhost:4000